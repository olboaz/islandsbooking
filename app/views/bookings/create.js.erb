const displayModal = () => {
  $('#booking-confirmation').modal({
  keyboard: false,
  show: true,
  backdrop: 'static'
});
}

setTimeout(displayModal, 500);

const redirectUser = () => {
  window.location.href="<%= user_path(current_user) %>"
}

setTimeout(redirectUser, 3500);
